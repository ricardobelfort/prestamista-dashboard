{
  "version": 2,
  "buildCommand": "npm run version:generate && npx ng build",
  "outputDirectory": "dist/prestamista-dashboard-ui",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}