{
  "version": 2,
  "buildCommand": "node generate-version.js && npx ng build",
  "outputDirectory": "dist/prestamista-dashboard-ui/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}