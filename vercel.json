{
  "version": 2,
  "buildCommand": "npm run version:generate && npm run build",
  "outputDirectory": "dist/prestamista-dashboard-ui",
  "framework": "angular",
  "env": {
    "NODE_ENV": "production"
  }
}